
# https://github.com/liuzhao1225/YouDub-webui仓库启动前的环境配置：

## 1. 操作系统
- Windows 10 / 11

## 2. FFmpeg 安装（安装目录随意，下面以D盘为例）
- **重要**：必须下载包含DLL文件的动态链接版本（full_build-shared），用于torchcodec等库的动态调用
- 下载地址：https://www.gyan.dev/ffmpeg/builds/packages/ffmpeg-8.0.1-full_build-shared.7z
- 解压到固定目录（如 `D:\Program Files\ffmpeg`）
- 将 `D:\Program Files\ffmpeg\bin` 添加到系统 PATH 环境变量
- 验证安装：打开 PowerShell 运行 `ffmpeg -version`
- **注意事项**：不要下载 static 版本，因为 static 版本不包含DLL文件，会导致torchcodec等依赖FFmpeg DLL的库无法正常工作

## 3. Python 版本管理（使用 pyenv-win）
- 安装 pyenv-win：
  ```powershell
  Invoke-WebRequest -UseBasicParsing -Uri "https://raw.githubusercontent.com/pyenv-win/pyenv-win/master/pyenv-win/install-pyenv-win.ps1" -OutFile "./install-pyenv-win.ps1"
  &"./install-pyenv-win.ps1"
  ```
- 配置环境变量：
  - 按 `Win + R`，输入 `sysdm.cpl`，回车
  - 点击"高级" → "环境变量"
  - 在"用户变量"中添加：
    - 变量名: `PYENV`，变量值: `%USERPROFILE%\.pyenv\pyenv-win`
    - 变量名: `PYENV_HOME`，变量值: `%USERPROFILE%\.pyenv\pyenv-win`
  - 在"用户变量"的 Path 中添加：
    - `%USERPROFILE%\.pyenv\pyenv-win\bin`
    - `%USERPROFILE%\.pyenv\pyenv-win\shims`
  - 点击"确定"保存，完全注销 Windows 并重新登录
- 重启终端后验证：`pyenv --version`

## 4. 修改 pyenv-win 镜像源（华为源）
- 修改文件：`C:\Users\你的用户名\.pyenv\pyenv-win\.versions_cache.xml`
- 修改操作：打开文件后按 `Ctrl + F` 全局替换
  - 将 `https://www.python.org/ftp/python` 替换为 `https://mirrors.huaweicloud.com/python`
- 注意：如果文件已经是华为源则无需修改
- 然后安装 Python 3.12.7：`pyenv install 3.12.7`
- 进入项目目录：`cd 你的项目路径`
- 设置项目使用 Python 3.12.7：`pyenv local 3.12.7`

## 5. 构建项目虚拟环境
- 进入项目目录：`cd 你的项目路径`
- 使用 pyenv 的 Python 3.12.7 创建虚拟环境：
  ```powershell
  C:\Users\你的用户名\.pyenv\pyenv-win\versions\3.12.7\tools\python.exe -m venv .venv
  .\.venv\Scripts\python.exe -m pip install -U pip
  .\.venv\Scripts\pip.exe install -i https://mirrors.aliyun.com/pypi/simple/ -r requirements.txt
  ```

## 6. 修改 list_models 函数
```python
def list_models(*, base_url: str, api_key: str) -> list[str]:
    if not api_key:
        raise ValueError("OpenAI API key is not configured.")
    
    # 智谱AI不支持 /models 接口，返回固定模型列表
    normalized_url = normalize_openai_base_url(base_url)
    if "bigmodel.cn" in normalized_url or "open.bigmodel.cn" in normalized_url:
        return [
            "glm-4.5-air",
            "glm-4.6v",
            "GLM-4.7-Flash",
            "GLM-4.6V-Flash",
            "GLM-4.1V-Thinking-Flash",
            "GLM-4-Flash-250414",
            "Cogview-3-Flash",
            "CogVideoX-Flash"
        ]
    
    # 其他支持 /models 的服务
    client = OpenAI(api_key=api_key, base_url=normalized_url)
    response = client.models.list()
    seen: set[str] = set()
    models: list[str] = []
    for item in response.data:
        model_id = getattr(item, "id", "")
        if model_id and model_id not in seen:
            seen.add(model_id)
            models.append(model_id)
    return models
```


# YouDub WebUI 环境配置报告

**生成时间**: 2026-05-22
**项目路径**: `d:\test_react\claude_test\youDub-project\YouDub-webui`

---

## 配置流程总结

本次环境配置过程中遇到的主要问题及解决方案：

### 1. FFmpeg 安装问题
**问题**: winget 命令无法识别，无法通过 winget 安装 FFmpeg
**原因**: PowerShell 环境问题或 winget 未正确安装
**解决**: 手动下载 FFmpeg 并配置到 PATH
- **重要**：必须下载包含DLL文件的动态链接版本（full_build-shared）
- 下载地址：https://www.gyan.dev/ffmpeg/builds/packages/ffmpeg-8.0.1-full_build-shared.7z
- 解压到 `D:\Program Files\ffmpeg`
- 将 `D:\Program Files\ffmpeg\bin` 添加到系统 PATH 环境变量
- 验证：`ffmpeg -version`
- **注意事项**：不要下载 static 版本，因为 static 版本不包含DLL文件，会导致torchcodec等依赖FFmpeg DLL的库无法正常工作

### 2. Git Clone 网络和权限问题
**问题**:
- Git clone 时出现 SSL 连接错误：`OpenSSL SSL_connect: SSL_ERROR_SYSCALL`
- 目录权限问题导致无法删除和重新克隆
**解决**:
- 网络问题：配置代理或手动下载 ZIP 包
- 权限问题：使用 `cmd /c "rmdir /s /q"` 强制删除目录
- 最终手动完成了克隆过程

### 3. demucs 子模块配置问题
**问题**: `git submodule update --init --recursive` 报错
```
error: could not lock config file submodule/demucs/.git/config: Permission denied
error: unable to write upstream branch configuration
```
**原因**: demucs 目录为空，Git 无法正确配置子模块
**解决**:
- 手动删除空的 demucs 目录
- 手动克隆 demucs：`git clone https://github.com/facebookresearch/demucs.git submodule/demucs`
- 手动配置上游分支：`git branch --set-upstream-to=origin/main`

### 4. Python 版本兼容性问题
**问题**: 初始使用 Python 3.13.9，导致 `editdistance` 包编译失败
**原因**: `editdistance` 包的 C++ 扩展与 Python 3.13 不兼容
**解决**: 安装 pyenv-win 并切换到 Python 3.12.7，重建虚拟环境

### 5. pyenv-win 环境变量配置
**问题**: 安装 pyenv-win 后命令不可用
**原因**: pyenv-win 未添加到系统 PATH 环境变量或环境变量未生效
**解决**: 手动配置环境变量或重启终端使环境变量生效

### 6. 虚拟环境版本不匹配
**问题**: 虚拟环境仍使用系统 Python 3.13.9
**原因**: 创建虚拟环境时使用了系统默认 Python 而非 pyenv 的 Python
**解决**: 使用 pyenv 的 Python 3.12.7 完整路径重建虚拟环境

### 7. C++ Build Tools 缺失
**问题**: 安装 Python 依赖时出现编译错误
```
error: Microsoft Visual C++ 14.0 or greater is required
```
**原因**: 缺少 C++ 编译器，无法编译需要 C++ 扩展的包（如 editdistance）
**解决**: 安装 Microsoft C++ Build Tools
- 下载地址：https://visualstudio.microsoft.com/visual-cpp-build-tools/
- 安装 "Desktop development with C++" 工作负载

### 8. 字符编码问题
**问题**: editdistance 编译时出现编码错误
```
warning C4819: 该文件包含不能在当前代码页(936)中表示的字符
```
**原因**: Windows 系统默认使用 GBK 编码，但源文件包含 UTF-8 字符
**解决**: 设置系统代码页为 UTF-8
```powershell
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
chcp 65001
```

### 9. PowerShell 路径动态变化问题
**问题**: VSCode 设置的 PowerShell 路径会随版本更新而变化
```
C:\Program Files\WindowsApps\Microsoft.PowerShell_7.6.2.0_x64__8wekyb3d8bbwe\pwsh.exe
```
**原因**: Windows Store 版本的 PowerShell 7 路径包含版本号，每次更新都会变化
**解决**: 使用命令名称而非完整路径
```json
"pwsh": {
  "path": "pwsh"  // 从 PATH 中查找
}
```
或使用稳定的 Windows PowerShell 5.1：
```json
"pwsh": {
  "path": "C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe"
}
```

### 10. .env 文件创建被阻止
**问题**: 尝试创建 .env 文件时被 gitignore 保护阻止
**原因**: .env 文件在 .gitignore 中被标记为忽略，工具无法创建
**解决**: 手动复制或创建 .env 文件
```powershell
Copy-Item env.txt.example .env
```

### 11. Demucs 进度条 Windows 兼容性问题
**问题**: 执行音频分离时报错 `OSError: [Errno 22] Invalid argument`
**原因**: Demucs 库使用 tqdm 显示进度条，在 Windows 环境下调用 `sys.stderr.flush()` 时出现兼容性问题
**解决**: 修改 `backend/app/adapters/demucs.py` 文件，禁用 Demucs 的进度条
```python
# 将 progress=True 改为 progress=False
separator = Separator(model="htdemucs_ft", device=_device(), progress=False, shifts=3)
```

### 12. Whisper 模型下载和配置
**问题**: Whisper 模型首次运行时自动下载，可能较慢或网络问题
**原因**: 默认从 OpenAI CDN 下载模型文件（large-v3-turbo 约 1.5GB）
**解决**: 手动下载模型并配置本地路径
- 模型下载地址：https://www.modelscope.cn/models/iic/Whisper-large-v3-turbo/file/view/master/large-v3-turbo.pt?status=2
- 将模型文件放到项目根目录的 `models` 文件夹
- 在 `.env` 文件中添加配置：
  ```env
  WHISPER_MODEL=large-v3-turbo
  WHISPER_DOWNLOAD_ROOT=./models
  ```

### 13. 智谱AI 翻译速率限制
**问题**: 翻译字幕时报错 `Error code: 429 - 您的账户已达到速率限制，请您控制请求频率`
**原因**: 默认翻译并发数过高（50），触发了智谱AI的速率限制
**解决**: 在 `.env` 文件中降低翻译并发数
```env
# 免费账户建议设置为 1，其实也挺快的
OPENAI_TRANSLATE_CONCURRENCY=1
```

### 14. VoxCPM Windows print 兼容性问题
**问题**: 生成配音时报错 `OSError: [Errno 22] Invalid argument`
**原因**: VoxCPM 库在初始化时调用 `print()` 函数，在 Windows 环境下出现兼容性问题
**解决**: 修改 `backend/app/adapters/voxcpm.py` 文件，在模型加载时临时重定向标准输出
```python
def _load_model():
    global _MODEL
    if _MODEL is None:
        import sys
        from voxcpm import VoxCPM

        # 临时重定向标准输出以避免Windows下的print兼容性问题
        original_stdout = sys.stdout
        original_stderr = sys.stderr
        try:
            import io
            sys.stdout = io.StringIO()
            sys.stderr = io.StringIO()
            
            _MODEL = VoxCPM.from_pretrained(
                str(_model_path()),
                load_denoiser=os.getenv("VOXCPM_LOAD_DENOISER", "false").lower() == "true",
            )
        finally:
            sys.stdout = original_stdout
            sys.stderr = original_stderr
    return _MODEL
```

---

## 一、当前环境状态

### ✅ 已安装的基础依赖

| 依赖项 | 版本/状态 | 说明 |
|--------|----------|------|
| **Python (系统)** | 3.12.7 (pyenv) | pyenv 已安装 3.12.7 |
| **Python (虚拟环境)** | 3.12.7 | ✅ 已使用 pyenv 的 Python 3.12.7 重建 |
| **pyenv-win** | 3.1.1 | ✅ 已安装并配置 |
| **Node.js** | v22.22.0 | 满足要求 (20+) |
| **FFmpeg** | 2026-05-21-git-0857141823 | 已安装并在 PATH 中 |
| **ffprobe** | 2026-05-21-git-0857141823 | 已安装并在 PATH 中 |
| **前端依赖** | 已安装 | `apps/web/node_modules` 存在 |

### ❌ 未安装或未配置的部分

#### Python 依赖包
虚拟环境中当前仅安装了 `pip 24.2`，`requirements.txt` 中的所有包需要安装。

**安装命令**:
```powershell
.\.venv\Scripts\pip.exe install -i https://mirrors.aliyun.com/pypi/simple/ -r requirements.txt
```

**需要安装的包**:
- fastapi
- uvicorn[standard]
- python-dotenv
- pydantic
- python-multipart
- openai
- yt-dlp[default]
- dora-search>=0.1.12
- diffq>=0.2.1
- julius>=0.2.3
- lameenc>=1.2
- openunmix
- openai-whisper
- spacy
- voxcpm
- soundfile
- pydub
- librosa
- audiostretchy
- numpy
- pytest
- httpx

#### 环境配置
`.env` 文件已存在，但以下关键配置需要设置：

| 配置项 | 说明 | 默认值/建议值 |
|--------|------|--------------|
| `OPENAI_BASE_URL` | OpenAI 兼容 API 地址 | `https://api.openai.com/v1` |
| `OPENAI_API_KEY` | 翻译阶段使用的 API key | **必须配置** |
| `OPENAI_MODEL` | 翻译模型名称 | `gpt-4o-mini` |
| `DEVICE` | 模型运行设备 | `cpu` |
| `YTDLP_PROXY_PORT` | yt-dlp 代理端口 | `7890` (处理 YouTube 需要配置) |
| `HTTP_PROXY` | HTTP 代理地址 | 根据实际情况配置 |

#### 代理设置
- **YouTube 访问代理**: 未设置（处理 YouTube 视频时需要）
- **YouTube Cookie**: 未配置（处理 YouTube 视频推荐配置）

---

## 二、启动服务命令

### 方式一：使用项目脚本（推荐）

项目根目录的 `package.json` 已配置快捷脚本：

```powershell
# 启动后端
npm run dev:api

# 启动前端
npm run dev:web
```

### 方式二：直接执行命令

```powershell
# 启动后端
.\.venv\Scripts\uvicorn.exe backend.app.main:app --reload --host 0.0.0.0 --port 8000

# 启动前端
npm --prefix apps/web run dev -- --hostname 0.0.0.0 --port 3000
```

### 访问地址

- **前端**: http://localhost:3000
- **后端**: http://localhost:8000

---

## 三、下一步操作建议

### 1. 安装 Python 依赖（必须）
```powershell
.\.venv\Scripts\pip.exe install -i https://mirrors.aliyun.com/pypi/simple/ -r requirements.txt
```

### 2. 配置环境变量（必须）
编辑 `.env` 文件，至少配置以下项：
```env
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_API_KEY=your_api_key_here
OPENAI_MODEL=gpt-4o-mini
DEVICE=cpu
```

### 3. 配置 YouTube 访问（可选，如需处理 YouTube 视频）
- 设置代理端口：`YTDLP_PROXY_PORT=7890`
- 导出 YouTube Cookie 并粘贴到设置页面

### 4. 启动服务
```powershell
# 终端1 - 启动后端
npm run dev:api

# 终端2 - 启动前端
npm run dev:web
```

---

## 四、注意事项

1. **Python 版本**: 虚拟环境已使用 Python 3.12.7 重新创建（避免 Python 3.13 的兼容性问题）
2. **依赖安装**: Python 依赖安装需要一定时间，请耐心等待
3. **模型下载**: 首次运行会下载较大的 ASR、TTS 模型，请预留磁盘空间
4. **代理配置**: 处理 YouTube 视频必须配置代理和 Cookie
5. **Python 版本管理**: 使用 pyenv-win 管理 Python 版本，避免版本冲突

---

## 五、常见错误及解决方案

### 1. Python 虚拟环境相关错误

#### 错误：pyenv 命令不可用
**现象**: 运行 `pyenv` 命令提示"不是内部或外部命令"

**原因**: pyenv-win 未添加到系统 PATH 环境变量或环境变量未生效

**解决方案**:
```powershell
# 方法1: 手动添加到 PATH（临时，当前会话有效）
$env:PATH = "$env:USERPROFILE\.pyenv\pyenv-win\bin;$env:USERPROFILE\.pyenv\pyenv-win\shims;$env:PATH"
pyenv --version

# 方法2: 使用完整路径调用
& "$env:USERPROFILE\.pyenv\pyenv-win\versions\3.12.7\tools\python.exe" --version

# 方法3: 手动配置系统环境变量（永久）
# 1. 按 Win + R，输入 sysdm.cpl，回车
# 2. 点击"高级" → "环境变量"
# 3. 在"用户变量"中添加：
#    - 变量名: PYENV
#    - 变量值: C:\Users\LENOVO\.pyenv\pyenv-win
#    - 变量名: PYENV_HOME
#    - 变量值: C:\Users\LENOVO\.pyenv\pyenv-win
# 4. 在"用户变量"的 Path 中添加：
#    - C:\Users\LENOVO\.pyenv\pyenv-win\bin
#    - C:\Users\LENOVO\.pyenv\pyenv-win\shims
# 5. 确定保存，完全注销 Windows 并重新登录

# 方法4: 重新运行安装脚本
.\install-pyenv-win.ps1
```

#### 错误：Python 版本不匹配
**现象**: 虚拟环境使用错误的 Python 版本（如 3.13.9 而非 3.12.7）

**原因**: 创建虚拟环境时使用了系统默认 Python 而非 pyenv 的 Python

**解决方案**:
```powershell
# 删除旧虚拟环境
Remove-Item -Recurse -Force .venv

# 使用 pyenv 的 Python 3.12.7 创建新虚拟环境
$env:USERPROFILE\.pyenv\pyenv-win\versions\3.12.7\tools\python.exe -m venv .venv

# 验证版本
.\.venv\Scripts\python.exe --version
```

#### 错误：pip 安装包失败
**现象**: `pip install` 时出现网络错误或包找不到

**原因**: 镜像源不可用或网络问题

**解决方案**:
```powershell
# 使用阿里云镜像
.\.venv\Scripts\pip.exe install -i https://mirrors.aliyun.com/pypi/simple/ -r requirements.txt

# 如果阿里云镜像失败，尝试清华镜像
.\.venv\Scripts\pip.exe install -i https://pypi.tuna.tsinghua.edu.cn/simple/ -r requirements.txt

# 单独安装失败的包
.\.venv\Scripts\pip.exe install -i https://pypi.tuna.tsinghua.edu.cn/simple/ 包名
```

### 2. 依赖包安装错误

#### 错误：editdistance 编译失败（Python 3.13 兼容性问题）
**现象**: 安装依赖时 `editdistance` 包编译失败，出现 C++ 编译错误
```
error: subprocess-exited-with-error
× Building wheel for editdistance (pyproject.toml) did not run successfully.
src/editdistance/_editdistance.cpp(140): error C2059: 语法错误:"if"
```

**原因**:
- `editdistance` 包的 C++ 扩展与 Python 3.13 不兼容
- Python 3.13 是较新版本，部分依赖包尚未完全支持
- 该包可能是某个依赖的间接依赖（如 `dora-search`）

**解决方案**:
```powershell
# 方案1: 使用 Python 3.12 替代 3.13（推荐）
# 1. 安装 pyenv-win
Invoke-WebRequest -UseBasicParsing -Uri "https://raw.githubusercontent.com/pyenv-win/pyenv-win/master/pyenv-win/install-pyenv-win.ps1" -OutFile "./install-pyenv-win.ps1"
&"./install-pyenv-win.ps1"

# 2. 重启终端后安装 Python 3.12.7
pyenv install 3.12.7

# 3. 在项目中设置 Python 3.12.7
cd d:\test_react\claude_test\youDub-project\YouDub-webui
pyenv local 3.12.7

# 4. 重建虚拟环境
Remove-Item -Recurse -Force .venv
$env:USERPROFILE\.pyenv\pyenv-win\versions\3.12.7\tools\python.exe -m venv .venv

# 5. 重新安装依赖
.\.venv\Scripts\pip.exe install -i https://mirrors.aliyun.com/pypi/simple/ -r requirements.txt

# 方案2: 使用预编译 wheel（如果必须使用 Python 3.13）
.\.venv\Scripts\pip.exe install --only-binary :all: editdistance

# 方案3: 使用替代包（如果 editdistance 不是必需的）
.\.venv\Scripts\pip.exe install rapidfuzz
# 然后在代码中将 editdistance 替换为 rapidfuzz
```

**预防措施**:
- 使用 pyenv-win 管理 Python 版本，避免使用过新的 Python 版本
- 推荐使用 Python 3.10 或 3.12，避免使用 3.13（除非明确需要）
- 检查依赖包的 Python 版本兼容性

#### 错误：openai-whisper 安装失败
**现象**: 安装 `openai-whisper` 时出现编译错误

**原因**: 缺少 C++ 编译器或 Rust 工具链

**解决方案**:
```powershell
# 安装 Microsoft C++ Build Tools
# 下载地址: https://visualstudio.microsoft.com/visual-cpp-build-tools/

# 或使用 winget 安装
winget install Microsoft.VisualStudio.2022.BuildTools

# 安装 Rust
winget install Rustlang.Rustup
```

#### 错误：torch/torchaudio 安装失败
**现象**: PyTorch 相关包安装失败或版本不兼容

**原因**: Python 版本与 PyTorch 版本不匹配

**解决方案**:
```powershell
# 先卸载已安装的 torch
.\.venv\Scripts\pip.exe uninstall torch torchaudio

# 使用官方推荐的安装命令
.\.venv\Scripts\pip.exe install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
```

#### 错误：spacy 模型下载失败
**现象**: spacy 安装成功但模型下载失败

**解决方案**:
```powershell
# 手动下载模型
.\.venv\Scripts\python.exe -m spacy download en_core_web_sm

# 如果下载失败，使用代理
$env:HTTP_PROXY = "http://127.0.0.1:7890"
.\.venv\Scripts\python.exe -m spacy download en_core_web_sm
```

### 3. 后端启动错误

#### 错误：ModuleNotFoundError
**现象**: 启动后端时提示找不到某个模块

**原因**: 依赖包未完全安装

**解决方案**:
```powershell
# 重新安装所有依赖
.\.venv\Scripts\pip.exe install -i https://mirrors.aliyun.com/pypi/simple/ -r requirements.txt --force-reinstall

# 检查特定包是否安装
.\.venv\Scripts\pip.exe list | findstr "包名"
```

#### 错误：.env 文件未找到
**现象**: 启动时提示找不到 .env 文件

**解决方案**:
```powershell
# 从示例文件复制
Copy-Item .env.example .env

# 或从 env.txt.example 复制
Copy-Item env.txt.example .env
```

### 4. 前端启动错误

#### 错误：端口被占用
**现象**: 前端启动时提示端口 3000 被占用

**解决方案**:
```powershell
# 查找占用端口的进程
netstat -ano | findstr :3000

# 结束进程（替换 PID）
taskkill /PID <进程ID> /F

# 或使用其他端口
npm --prefix apps/web run dev -- --port 3001
```

#### 错误：依赖安装失败
**现象**: `npm install` 时出现错误

**解决方案**:
```powershell
# 清除 npm 缓存
npm cache clean --force

# 删除 node_modules 重新安装
Remove-Item -Recurse -Force apps/web/node_modules
npm --prefix apps/web install --registry=https://registry.npmmirror.com
```

### 5. YouTube 下载错误

#### 错误：yt-dlp 下载失败
**现象**: YouTube 视频下载失败

**解决方案**:
```powershell
# 检查代理配置
# 编辑 .env 文件，设置 YTDLP_PROXY_PORT=7890

# 更新 yt-dlp
.\.venv\Scripts\pip.exe install --upgrade yt-dlp

# 使用代理测试
$env:HTTP_PROXY = "http://127.0.0.1:7890"
.\.venv\Scripts\yt-dlp.exe --version
```

#### 错误：Cookie 无效
**现象**: YouTube Cookie 导入后仍无法下载

**解决方案**:
```powershell
# 重新导出 Cookie
# 1. 安装 "Get cookies.txt LOCALLY" 浏览器扩展
# 2. 访问 YouTube 并登录
# 3. 点击扩展图标导出 Cookie
# 4. 在 YouDub WebUI 设置页面重新导入
```

### 6. FFmpeg 相关错误

#### 错误：FFmpeg 未找到
**现象**: 提示找不到 ffmpeg 或 ffprobe

**解决方案**:
```powershell
# 检查 FFmpeg 是否在 PATH 中
ffmpeg -version
ffprobe -version

# 如果未安装，使用 winget 安装
winget install Gyan.FFmpeg

# 重启终端使 PATH 生效
```

#### 错误：FFmpeg 版本过低
**现象**: FFmpeg 功能不完整

**解决方案**:
```powershell
# 升级 FFmpeg
winget upgrade Gyan.FFmpeg

# 或手动下载最新版本
# https://www.gyan.dev/ffmpeg/builds/
```

---

## 六、故障排查清单

### 后端启动失败
- [ ] 检查 Python 版本是否为 3.12.7
- [ ] 检查 Python 依赖是否完全安装
- [ ] 检查 `.env` 文件配置是否正确
- [ ] 检查 FFmpeg 是否在 PATH 中
- [ ] 查看错误日志确认具体问题

### 前端启动失败
- [ ] 检查 Node.js 版本是否 >= 20
- [ ] 删除 `apps/web/node_modules` 重新安装依赖
- [ ] 检查端口 3000 是否被占用
- [ ] 检查 npm 缓存是否需要清理

### YouTube 下载失败
- [ ] 确认代理配置正确
- [ ] 确认 Cookie 有效且未过期
- [ ] 检查网络连接
- [ ] 更新 yt-dlp 到最新版本

### 模型加载失败
- [ ] 检查磁盘空间是否充足
- [ ] 检查网络连接是否正常
- [ ] 检查代理配置是否正确
- [ ] 尝试手动下载模型

---

**报告结束**
