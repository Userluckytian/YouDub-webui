# 大模型文件下载说明

由于大模型文件体积较大（总计约6.2GB），GitHub仓库中不包含这些文件。首次运行项目前，需要手动下载并配置以下模型。

## 需要下载的模型

### 1. Whisper 语音识别模型（约1.5GB）

**下载地址**：
- ModelScope（推荐，国内速度快）：https://www.modelscope.cn/models/iic/Whisper-large-v3-turbo/file/view/master/large-v3-turbo.pt?status=2
- OpenAI CDN：https://openaipublic.azureedge.net/main/whisper/models/aff26ae408abcba5fbf8813c21e62b0941638c5f6eebfb145be0c9839262a19a/large-v3-turbo.pt

**放置目录**：
```
YouDub-webui/models/large-v3-turbo.pt
```

**环境变量配置**（在`.env`文件中）：
```env
WHISPER_MODEL=large-v3-turbo
WHISPER_DOWNLOAD_ROOT=./models
```

---

### 2. VoxCPM 语音合成模型（约4.7GB）

**下载地址**：
- ModelScope：https://www.modelscope.cn/models/OpenBMB/VoxCPM2

**放置目录**：
```
YouDub-webui/data/modelscope/OpenBMB__VoxCPM2/
```

**说明**：
- 从ModelScope下载整个模型文件夹
- 包含文件：audiovae.pth、model.safetensors、config.json、tokenizer.json等

**环境变量配置**（在`.env`文件中）：
```env
# 模型ID
VOXCPM_MODEL=OpenBMB/VoxCPM2

# 是否加载降噪器（false=不加载，提高速度；true=加载，提高音质）
VOXCPM_LOAD_DENOISER=false

# 推理参数（保持默认即可）
VOXCPM_CFG_VALUE=2.0
VOXCPM_INFERENCE_TIMESTEPS=10
VOXCPM_MIN_REFERENCE_MS=1200
```

**手动下载配置（仅在自动下载失败时需要）**：
如果代码运行时自动下载失败，可以手动下载并配置：
1. 从ModelScope下载VoxCPM2模型文件
2. 将下载的模型文件夹解压到任意目录，例如：`D:\VoxCPM2`
3. 在`.env`文件中添加：
   ```env
   VOXCPM_MODEL_DIR=D:\VoxCPM2
   ```
4. 重启后端服务

---

## 自动下载说明

**注意**：VoxCPM模型在代码运行时会自动从ModelScope下载，一般情况下不需要手动下载。如果网络环境正常，代码会自动处理下载和缓存。

Whisper模型也可以在首次运行时自动下载，但手动下载可以节省首次运行时间。

---

## 加速下载建议

如果下载速度较慢，可以使用以下方法加速：

### 使用Xget Now浏览器插件（推荐）
- **Edge插件**：https://microsoftedge.microsoft.com/addons/detail/xget-now/jigpfhbegabdenhihpplcjhpfdcgnalc?hl=zh-CN
- **Chrome插件**：https://chromewebstore.google.com/detail/xget-now/ajiejgobfcifcikbahpijopolfjoodgf?hl=zh-CN

**使用方法**：
1. 关闭其他代理软件
2. 打开浏览器插件
3. 开始下载模型或文件

---

## 磁盘空间需求

- Whisper模型：约1.5GB
- VoxCPM模型：约4.7GB
- 总计：约6.2GB

建议预留至少10GB磁盘空间。

---

## 验证下载

下载完成后，检查以下目录结构：

```
YouDub-webui/
├── models/
│   └── large-v3-turbo.pt          # Whisper模型（约1.5GB）
└── data/
    └── modelscope/
        └── OpenBMB__VoxCPM2/       # VoxCPM模型（约4.7GB）
            ├── audiovae.pth
            ├── model.safetensors
            ├── config.json
            ├── tokenizer.json
            └── ...
```

---

## 常见问题

### 1. 下载速度慢
- 使用Xget Now浏览器插件加速
- 或配置代理后使用ModelScope下载

### 2. 模型文件损坏
- 重新下载模型文件
- 验证文件大小是否与预期一致

### 3. 自动下载失败
- 检查网络连接
- 尝试手动下载并配置本地路径
- 参考上面的"手动下载配置"部分

---

**生成时间**：2026-05-26
