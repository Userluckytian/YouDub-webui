# 魔搭平台使用踩坑记录

## 环境信息

| 项目 | 说明 |
|------|------|
| 魔搭实例 | GPU 环境，8核32GB，24G显存，Ubuntu 22.04 |
| 项目目录 | `/mnt/workspace/YouDub-webui` |
| 前端端口 | 3000 |
| 后端端口 | 8000（通过 Next.js rewrites 代理） |

## 最终方案：Cloudflare Tunnel（cftunnel）

### 完整配置

#### 第 1 步：安装 cftunnel

```bash
curl -fsSL https://raw.githubusercontent.com/qingchencloud/cftunnel/main/install.sh | bash
```

#### 第 2 步：Next.js 配置说明

##### 关键配置

在 `apps/web/next.config.ts` 中，需要正确配置 API 代理：

```typescript
function getApiBaseUrl() {
  // 优先使用环境变量
  const configured =
    process.env.NEXT_SERVER_API_BASE_URL ||
    process.env.NEXT_PUBLIC_API_BASE_URL;
  
  if (configured) {
    return configured.replace(/\/$/, "");
  }
  
  // 开发/隧道环境：通过环境变量设置后端隧道地址
  // 生产环境：使用相对路径（走 rewrites 代理）
  return "";
}

const nextConfig: NextConfig = {
  basePath: '',
  allowedDevOrigins: ['*.trycloudflare.com', 'localhost'],
  reactStrictMode: false,
  
  // 只在没有设置 API_BASE_URL 时才启用 rewrites 代理
  async rewrites() {
    const apiBaseUrl = getApiBaseUrl();
    
    // 如果设置了环境变量，说明要直连后端，不启用代理
    if (apiBaseUrl && apiBaseUrl.startsWith('http')) {
      return [];
    }
    
    // 否则使用代理
    return [
      {
        source: "/api/:path*",
        destination: "http://127.0.0.1:8000/api/:path*",
      },
    ];
  },
};
```

##### 配置说明

1. **allowedDevOrigins**：添加了 `*.trycloudflare.com` 以允许 Cloudflare Tunnel 域名访问
2. **API 代理逻辑**：
   - 如果设置了 `NEXT_PUBLIC_API_BASE_URL` 环境变量，直接连接后端
   - 否则使用 Next.js rewrites 代理到本地 8000 端口
3. **生产模式**：使用相对路径走代理，避免跨域问题



### 安装后的日常使用流程

每次重启魔搭实例后，按顺序执行：

```bash
# 1. 进入项目目录
cd /mnt/workspace/YouDub-webui

# 2. 启动后端
.venv/bin/uvicorn backend.app.main:app --reload --host 0.0.0.0 --port 8000

# 3. 新终端：启动前端（生产模式）
npm --prefix apps/web run dev -- --hostname 0.0.0.0 --port 3000

# 4. 新终端：启动隧道
cftunnel quick 3000
```

然后访问生成的 `https://xxxx.trycloudflare.com` 即可。（如果不行，就把生成的https部分换成http访问
